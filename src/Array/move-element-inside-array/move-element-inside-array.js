/**
 * Напишите функцию moveElementInsideArray, которая в качестве первого аргумента принимает массив строк,
 * в качестве второго аргумента принимает индекс элемента котороый нужно перенести, третьим аргументом индекс куда перенести,
 * при этом элементы в массив должны сдвигаться. Вернуть новый массив с перестановкой
 *
 * Пример 1:
 * array = ['1', '2', '3', '4', '5'];
 * indexFrom = 1;
 * indexTo = 3
 *
 * moveElementInsideArray(array, index, element)
 *
 * Результат: ['2', '3', '4','1' '5'];
 *
 * Пример 2:
 * array = ['1', '2', '3', '4', '5'];
 * indexFrom = 3;
 * indexTo = 1
 *
 * moveElementInsideArray(array, index, element)
 *
 * Результат: ['1', '4', '2', '3', '5'];
 */

function moveElementInsideArray() {}

export { moveElementInsideArray };
